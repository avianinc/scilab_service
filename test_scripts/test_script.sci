// Coefficients
a = 1;
b = -3;
c = 2;

// Calculate the discriminant
D = b^2 - 4*a*c;

// Calculate the roots
root1 = (-b + sqrt(D)) / (2*a);
root2 = (-b - sqrt(D)) / (2*a);

// Display the results
disp("Root 1: ");
disp(root1);
disp("Root 2: ");
disp(root2);